# Raw SQL

Ad-hoc SQL scripts (reporting queries, one-off data fixes, index
tuning). EF Core migrations are the source of truth for schema changes
- see backend/MicroLIMS.Persistence/Migrations instead.
